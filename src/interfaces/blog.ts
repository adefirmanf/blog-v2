export default interface Blog{
    id: string;
        documentId: string;
        title: string;
        createdAt: string;
        updatedAt: string;
        publishedAt: string;
        published: boolean;
        blog_markdown: string;
}