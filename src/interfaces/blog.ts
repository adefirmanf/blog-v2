export default interface Blog{
    id: string;
        documentId: string;
        title: string;
        createdAt: string;
        updatedAt: string;
        publishedAt: string;
        published: boolean;
        deletedAt: string;
        blog_markdown: string;
}
